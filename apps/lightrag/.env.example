# --- LightRAG Configuration ---
# API Keys (Supports OpenAI or Gemini via litellm)
OPENAI_API_KEY="sk-..."

# --- PostgreSQL Configuration (AWS Aurora IAM Auth) ---
AWS_REGION="us-east-1"
POSTGRES_HOST="kompany.cluster-cml688qeuggm.us-east-1.rds.amazonaws.com"
POSTGRES_PORT="5432"
POSTGRES_USER="postgres"
POSTGRES_DATABASE="postgres"

# In Local Dev: Paste the IAM auth token from the AWS Console here.
# In Production: Leave this blank or completely remove it. The system will generate it via boto3.
POSTGRES_PASSWORD=""

# --- Workspace Configuration ---
LIGHTRAG_WORKSPACE="./rag_workspace"
